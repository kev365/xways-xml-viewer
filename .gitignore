# MSVC build artifacts (regenerated by build.bat)
*.obj
*.lib
*.exp
*.res
*.dll
*.pdb
*.ilk
*.idb

# Deployment bundle staged by build.bat — the DLL ships via GitHub Releases.
xtensions/

# WebView2 SDK (NuGet, fetched once — see README)
vendor/

# Live sidecar config — only *.cfg.example is tracked.
*.cfg
!*.cfg.example

# Release archives and backups
*.zip
*.7z
*.bak

# IDE and OS files
.vs/
.vscode/
.idea/
*.suo
*.user
Thumbs.db
desktop.ini
.DS_Store

# Local-only files
CLAUDE.md
.claude/
